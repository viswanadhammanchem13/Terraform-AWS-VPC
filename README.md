# Terraform-AWS-VPC

* This Module is developed by MAnchem Viswanadhem for VPC practice, All projects are created inside this VPC.

## Inputs
* project(required) - users shuold specify their project name
* environment(required) - users should specify an environment
* cidr.block(optinal) - users can prodie theire CIDR-block, default is 10.0.0.0/16